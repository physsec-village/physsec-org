# Keep the populated file on the VPS, outside every preview checkout.
# Cloudflare's published test keys are appropriate for previews.
TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=
TURNSTILE_ALLOWED_HOSTNAMES=pr-0.physsec.org

# Preview deployments force the store off and override the public origin.
STORE_ENABLED=false
