# Bootstrap configuration for a no-downtime migration from the legacy
# single-container deployment. The switching helper replaces this file after
# the first blue/green backend is healthy.
# slot: legacy
upstream psv_backend {
    server 127.0.0.1:8080;
    keepalive 32;
}
